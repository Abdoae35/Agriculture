@model LocationAddVm

@{
    ViewBag.Title = "Add New Location";
    Layout = "_Layout";
}

<form asp-action="Create" method="post" class="p-4 bg-light rounded shadow-sm" style="max-width: 500px; margin: auto;">
    @Html.AntiForgeryToken()

    <h4 class="mb-4 text-center">Add New Location</h4>

    <div class="form-group mb-3">
        <label class="form-label">Location Name</label>
        <input asp-for="Name" class="form-control" placeholder="Enter location name..." />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="form-group mb-4">
        <label class="form-label">Select Location Type</label>
        <select asp-for="LocationType" asp-items="ViewBag.TypesList" class="form-select">
            <option value="">-- Please select an option --</option>
        </select>
        <span asp-validation-for="LocationType" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary w-100" style="background-color: #14ae64">Submit</button>
</form>